(function () {
  const skeleton = {}
  let config = {
    device: "<meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">"
  }
  skeleton.init = function (e) {

  }

  window.skeleton = skeleton
})(window)
